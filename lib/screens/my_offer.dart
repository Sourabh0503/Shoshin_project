import 'package:flutter/material.dart';

class MyOfferScreen extends StatelessWidget {
  const MyOfferScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("My Offer Screen"));
  }
}
